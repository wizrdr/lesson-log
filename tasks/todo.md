# Lesson Log — план реализации

Источник идеи: карточка «Lesson Log» в прожарке «Десять выживших» (05.09.2026).
Решения 05.09: код сразу, без фазы разговоров; первые пользователи — Максим и жена; код пишут агенты, Максим ревьюит и проверяет на реальных уроках.

## Что строим (v1 для двух пользователей)

Студент загружает запись урока с репетитором → транскрипт → LLM вытаскивает исправления, новые слова и правила в накопительный журнал → карточки SRS до следующего урока → экран «перед уроком» с открытыми ошибками.

**Не в v1:** захват вкладки Zoom из браузера, push, share-ссылка репетитору, оплата, лендинг. Всё это появляется только после двух недель реального использования вдвоём.

## Решения

| Вопрос | Решение | Почему |
|---|---|---|
| Ввод аудио | Только загрузка файла: `audio_only.m4a` из локальной записи Zoom, Voice Memos с iPhone для очных уроков | getDisplayMedia со звуком работает только в Chrome на десктопе, iPhone во время звонка не пишет вообще. Загрузка покрывает оба кейса без хаков |
| Транскрипция | **Deepgram Nova-3 (`language=multi`) с callback**, Whisper как запасной вариант; решается спайком | Whisper API режет файлы >25 МБ (час m4a ≈ 30–60 МБ) и плохо держит смесь PL/RU. Deepgram берёт файл по signed URL до 2 ГБ и сам дёргает наш webhook, Edge Function не упирается в лимит 150 с |
| Извлечение | Claude Sonnet 5, structured output по JSON-схеме `{type, original, corrected, rule, quote}` | Точность важнее цены: ошибочные карточки убивают доверие. Haiku 4.5 сравнить в спайке |
| Аудио после обработки | Удаляется, хранится только транскрипт | Supabase Storage free = 1 ГБ ≈ 20 уроков. Плюс приватность |
| Данные на клиенте | Обычный Supabase + RLS, без Dexie-синка | Пайплайн серверный по природе, local-first ничего не даёт двум пользователям и добавляет слой синка |
| SRS | `ts-fsrs` | Готовый FSRS, свой алгоритм писать незачем. В Dayline SRS нет, переиспользовать нечего |
| Из Dayline берём | токены `tokens.css` + примитивы `src/ui`, magic-link auth, каркас Edge Functions, PWA-конфиг, деплой на GitHub Pages, позже функцию `remind` (web-push уже есть) | Личный код, правило «без рабочего кода» не нарушается |
| Стек | React 19 + Vite + TS + Tailwind v4, Supabase (Postgres, Auth, Storage, Edge Functions), GitHub Pages | Тот же, что в Dayline: агенты уже знают конвенции |
| Где Supabase | Схема `lesson_log` в проекте dayline (ref ghuhochssdbzyxextlso), Edge Functions с префиксом `ll-`, миграции через `db push --include-all` | Free-тариф: два активных проекта уже заняты (dayline, calibrator). Пользователи те же, auth общая. Решение 05.09 |
| Согласие репетитора | Чекбокс «репетитор предупреждён» на каждого репетитора, один раз | Запись без согласия — юридический и этический риск |

## Модель данных

- `tutors` — id, user_id, name, language (`pl`/`en`), consent_at
- `lessons` — id, user_id, tutor_id, date, audio_path (nullable после обработки), transcript, status (`uploaded` → `transcribing` → `extracting` → `ready` / `failed`), error
- `entries` — id, lesson_id, user_id, type (`correction` / `vocab` / `rule`), original, corrected, explanation, quote, deleted_at
- `cards` — entry_id, FSRS-поля (`due`, `stability`, `difficulty`, `reps`, `lapses`, `state`), last_review
- RLS: всё по `user_id = auth.uid()`
- `cards` дублирует `user_id` вместо join на `entries`: политика остаётся одной строкой, индекс `(user_id, due)` отдаёт due-карточки без join, а пишет обе таблицы одна функция `ll-extract`, так что расхождение невозможно. FSRS-поля добавлены до полного `Card` из `ts-fsrs` (`elapsed_days`, `scheduled_days`, `learning_steps`), иначе карточку нельзя восстановить без потерь.
- Все таблицы в схеме `lesson_log` общего с Dayline Supabase-проекта (решение 05.09), storage bucket `audio` общий, путь `{user_id}/{lesson_id}.m4a`.

## Пайплайн

1. Клиент грузит файл в Storage `audio/{user}/{lesson}.m4a`, создаёт `lessons` со статусом `uploaded`.
2. Edge Function `transcribe`: signed URL → Deepgram с `callback=<url функции extract>`; статус `transcribing`.
3. Edge Function `extract` (webhook Deepgram): сохраняет транскрипт → Claude → `entries` + `cards` → статус `ready` → удаляет аудио.
4. Клиент подписан на `lessons` через Realtime, показывает прогресс.

## Спринты

Каждый спринт: агенты пишут код в фоне, Максим ревьюит diff и делает ОДНУ решающую проверку на реальном пути. Без неё спринт не закрыт. Слоты: чт 18:00–19:00 и сб 2–3 ч с 14.09; спайк можно раньше, ему нужна только запись урока и два API-ключа.

### S0 — спайк (гейт, до кода продукта)
**Блокеры на Максиме (05.09):** нет ни одного API-ключа (Deepgram, OpenAI, Anthropic) и нет записи урока. Скрипты готовятся заранее, запуск — после ближайшего урока.
- [ ] Записать один реальный урок (Zoom local recording или Voice Memos)
- [x] Скрипт: файл → Deepgram Nova-3 `multi` и Whisper → два транскрипта (`spike/transcribe.mjs`, 05.09; dry-run ок, реальный API не гонялся — нет ключей)
- [x] Скрипт: транскрипт → Sonnet 5 и Haiku 4.5 → JSON исправлений (`spike/extract.mjs` + `compare.mjs`, 05.09; реальный API не гонялся)
- [ ] ПРОВЕРКА: Максим читает оба транскрипта и оба списка. Критерий: ≥7 из 10 извлечённых пунктов — реальные исправления репетитора, и в списке есть ошибка, которую он сам запомнил с урока
- [ ] Зафиксировать выбор транскрипции и модели в таблице решений
- **Kill:** транскрипт смеси PL/RU нечитаем у обоих провайдеров → идея умирает или сужается до английского

### S1 — каркас и пайплайн до первого журнала
- [x] Scaffold из Dayline: Vite + tokens + ui + auth (email+пароль, как в Dayline) + PWA + Actions на GitHub Pages; `CLAUDE.md` репо с правилами (05.09; lint/test/build зелёные, 14 тестов; e2e и деплой не гонялись)
- [x] Миграции `20260905000000_init.sql` и `20260905000100_transcribe_meta.sql` применены в проект dayline (05.09, через `scripts/db-migrate.sh`, т.к. `db push` не работает при общей истории). Схема `lesson_log` открыта в PostgREST, redirect URL добавлен в Auth, репо github.com/wizrdr/lesson-log, Pages задеплоен: https://wizrdr.github.io/lesson-log/ — экран входа проверен в Playwright, запрос к Auth доходит
- [x] Edge Functions `ll-transcribe` и `ll-extract` написаны, 23 Deno-теста зелёные, `deno check` чистый (05.09). НЕ задеплоены, секреты не заданы, реальный путь Deepgram → callback → Claude не гонялся
- [x] Экраны: список уроков, шторка загрузки с репетитором и согласием, страница урока со статусом, ретраем и entries по типам; realtime (05.09; 26 vitest зелёных, build чистый; в браузере и на реальном Supabase НЕ проверено)
- [ ] ПРОВЕРКА: Максим на задеплоенном сайте с iPhone грузит реальную запись → через несколько минут видит entries. Аудио в Storage удалено

### S1.5 — дизайн и язык (добавлено 05.09 по просьбе Максима)
- [x] Три направления на канве Claude Design (Тетрадь / Консоль / Карточки), выбрана «Тетрадь» (05.09)
- [x] «Тетрадь» внедрена: токены, Literata + Golos Text, примитивы, Shell, экраны списка и урока; проверено в браузере на 390×844 в светлой и тёмной теме с тестовым аккаунтом `ll-test@example.com` (данные посеяны SQL; пароль в scratchpad сессии, при потере — пересоздать через временное включение регистрации)
- [x] Мультиязычность интерфейса ru/en: свой лёгкий i18n (`src/i18n`), переключатель на входе и в шапке уроков; проверено в браузере (05.09, 45 тестов). Язык объяснений в извлечении остаётся русским (промпт `ll-extract`), это отдельное решение
- [x] Адаптация под планшет и десктоп: ≥768 боковая колонка навигации, ≥1024 разворот «список | урок», Sheet как диалог, переключатель языка в сайдбаре; проверено в браузере на 390/834/1440 (05.09, 56 тестов). Manifest orientation → any
- [ ] Синхронизация iPhone ↔ Mac: отдельной работы не требует, данные в Supabase + realtime; подтвердить на реальной загрузке в проверке S1

### S1.6 — косяки вёрстки (ревью 05.09: WebKit iPhone 14 / 375×667, планшет и десктоп, аудит кода)
Скриншоты и замеры: scratchpad сессии `qa-mobile/`, `qa-desktop/`.

**Блок 1 — мешает пользоваться (телефон)**
- [ ] 1. Статус-бар iOS: `black-translucent` + safe-area padding внутри скроллера → контент уезжает под часы, часы белые на светлой бумаге. Фикс: `default` в index.html, padding и `bg-bg` на корень Shell вне `main`
- [ ] 2. Шторки и фокус: `autoFocus` в формах перебивается фокус-трапом Sheet → клавиатура мигает, остаётся сдвиг viewport («подскролл»); диалог не держит Tab и не возвращает фокус после закрытия
- [ ] 3. Шторки не помещаются: правка карточки выше области на 18–20 px (микроскролл), «Загрузить» с новым репетитором 546 px в 445; поля «Слово» и «Перевод» однострочные, длинная фраза видна на 55 % → textarea с автовысотой
- [ ] 4. Фильтры журнала: Segmented 362 px шире экрана 375, скрытый горизонтальный скролл = второй скроллер, «Правила» обрезаны. Фикс: Segmented на всю ширину с `flex-1`, убрать обёртку `overflow-x-auto`
- [ ] 5. Кнопка «Повторить»: обрезает текст без тени/hairline, висит на 40 px выше таббара, на коротком уроке стоит посреди экрана, сплошной фон режет линовку; на десктопе 600–672 px. Фикс: `.paper` flex-col, Page `flex-1`, кнопка `mt-auto`, hairline сверху, max-width на десктопе
- [ ] 6. Линовка 32 px не совпадает со строками (79/97/101/119 px) → двойные линии у границ, линии режут текст. Главный источник «всё не в попад». Решить: шаг 24 px + `leading-6` у serif-строк, либо линовка только на пустых областях
- [ ] 7. Длинные польские слова: нет `overflow-wrap`/`hyphens`, сетка слов 2 колонки на телефоне → перекрытие. Фикс: `grid-cols-1 sm:grid-cols-2`, `wrap-anywhere`, `lang="pl"` на польском тексте
- [ ] 8. Кнопка «Добавить карточку» переносится в две строки внутри пилюли; в шапках `items-end` при кнопках 44 px и H1 36 px → кнопки торчат выше заголовка. Фикс: `whitespace-nowrap` в Button, `items-center`, на телефоне короткая метка
- [ ] 9. Статистика повторения: «К ПОВТОРЕНИЮ» переносится в колонке 98 px, цифры на разной высоте
- [ ] 10. Инпуты 15 px + `maximum-scale=1` (гасит зум при фокусе, но выключает pinch-zoom в PWA). Фикс: 16 px в инпутах, убрать `maximum-scale`
- [ ] 11. Шрифты с Google Fonts не кешируются SW → при холодном старте текст в SF/Georgia, потом swap и «плывут» ширины. Фикс: woff2 в `public/fonts` + `@font-face`

**Блок 2 — десктоп и планшет**
- [ ] 12. Список и урок скроллятся вместе одной страницей (единственный скроллер `main`); нужны независимые скроллы панелей
- [ ] 13. На 1024 колонка урока 392 px против 640 на 1023 — скачок; двухпанельный режим включать с ~1200 или список уже
- [ ] 14. Мигание 140–160 мс при 1023↔1024: LessonPage ремаунтится и перегружает данные
- [ ] 15. Планшет: переключатель языка только на «Уроках», на остальных разделах язык не сменить; ссылка «‹ Уроки» лишняя при боковой навигации; контент прижат к nav (на 1000 px 256 px пустоты справа)
- [ ] 16. Плейсхолдер «Выбери урок слева» в левом верхнем углу, не на линии заголовка; заголовки списка и урока на разной высоте (28 vs 20)

**Блок 3 — система отступов и контраст**
- [ ] 17. Горизонтальные отступы гуляют: страницы 24, шторки 20, строки журнала `pl-6 pr-3`, таббар 8; hairline на уроке inset 24, в списке и журнале full-bleed; иконки-тогглы на трёх разных осях (30/34/36 px от края). Одна система: 24 везде, линии full-bleed, тогглы `pr-3 size-11`
- [ ] 18. Контраст `--text-faint` 3.46:1 на светлой бумаге (подписи таббара, капсы секций, интервалы, «Цитата»). Фикс: `#7a6e64`
- [ ] 19. Нативный `input[type=file]` по-английски и без стилей → своя кнопка «Выбрать файл» + имя файла; Select без стрелки; date-input центрирует значение на iOS
- [ ] 20. Список уроков на 375: колонка даты 104 px → «Agnieszka · польский» в две строки

**Блок 4 — мелочи**
- [ ] 21. Тап-таргеты 36 px у тогглов колоды и сегментов → 44
- [ ] 22. `theme-color` для тёмной темы, `apple-touch-icon` 180, сплэш, `lang` манифеста
- [ ] 23. 404 в консоли при холодном открытии глубоких ссылок на GitHub Pages
- [ ] 24. Long-press на строке урока открывает link-preview iOS
- [ ] 25. Пустое состояние повторения: две фразы склеены без точки
- [ ] 26. `body.sheet-open { overflow:hidden }` ничего не делает (скроллер — main); `min-h-dvh` в LessonsLayout даёт лишние 60 px скролла на десктопе
- [ ] Проверить на реальном iPhone: ряд из четырёх оценок на 375 (WebKit влезает, расчёт по коду говорит впритык)

### S2 — журнал и повторение
- [x] Журнал: все entries по урокам + группа «Мои карточки», фильтр по типу, поиск, правка/удаление, переключатель «в колоде» (05.09; проверено в браузере на реальной БД)
- [x] Повторение: `ts-fsrs`, лицо/переворот, Снова/Трудно/Хорошо/Легко с интервалами, клавиши 1–4; ручные карточки (`entries.lesson_id` nullable, миграция 20260905000200 применена); проверено: оценка записала FSRS-поля в БД (05.09, 92 теста)
- [ ] Экран «перед уроком»: due-карточки + топ повторяющихся ошибок по репетитору
- [x] Правка и удаление entries — через журнал (05.09)
- [ ] Второй аккаунт: жена, свой репетитор
- [ ] ПРОВЕРКА: оба проходят повторение на iPhone перед реальным уроком; на уроке хотя бы одна карточка «сработала» (ошибка не повторилась) — записать

### S3 — две недели реального использования
- [ ] Лог использования: дата, кто, загрузил ли урок, прошёл ли повторение, что раздражало (файл `tasks/usage-log.md`)
- [ ] Чинить только то, что попало в лог
- [ ] Push за час до урока через `remind` из Dayline — только если в логе оба написали «забыл повторить»
- [ ] Запись с микрофона в браузере (MediaRecorder) для очных уроков — только если загрузка Voice Memos оказалась раздражающей
- [ ] ПРОВЕРКА: ≥4 урока загружено на двоих, оба ответили на вопрос «продолжим пользоваться без напоминаний от Максима?»

### S4 — наружу (только если S3 «да»)
- [ ] Lemon Squeezy: $9/мес, 3 урока бесплатно; лендинг; шаблон сообщения репетитору
- [ ] Пост в чатах «Польский язык Варшава» / релокантских / r/learnpolish: 5 тестеров
- [ ] Метрика Gate 0: первый платящий ИЛИ 10 активных

## Kill-критерии
- S0 не пройден.
- После S3 хотя бы один из двух пользователей говорит «не буду».
- Тратим на разбор ошибок извлечения больше времени, чем экономим на конспектах.

## Открытые вопросы
- Deepgram vs Whisper на PL/RU — закрывается в S0.
- Haiku 4.5 достаточно для извлечения? — S0.
- Лимит 150 с у Edge Function на LLM-шаг для 90-минутного урока — замерить в S1; запасной план: очередь через pg_cron.
- Storage free: максимум 50 МБ на файл. Замерить реальный размер записи урока в S0; если больше — сжатие на клиенте или загрузка напрямую в Deepgram через функцию.
- Аккаунт жены: регистрация в общем проекте выключена, приглашение через Dashboard → Auth → Invite user (S2). Redirect URL `https://wizrdr.github.io/lesson-log/**` добавить в Auth → URL Configuration перед деплоем.

## Ревью
_(заполняется по закрытии спринтов: что сработало, что нет, что поменяли)_

## Где остановились — 05.09.2026, ночь

**Закоммичено и задеплоено:** https://wizrdr.github.io/lesson-log/ (вход работает до Supabase), схема `lesson_log` в проекте dayline, функции `ll-transcribe`/`ll-extract` задеплоены, `CALLBACK_TOKEN` задан.

**Ждёт Максима:** ключи Deepgram и Anthropic → `npx supabase secrets set DEEPGRAM_API_KEY=… ANTHROPIC_API_KEY=…`; запись первого урока. Аккаунт: тот же, что в Dayline (проект общий); аккаунт жены — Dashboard → Auth → Invite user (регистрация выключена).

**Решающая проверка S1 (не сделана):** войти на сайте с iPhone, загрузить реальную запись, дождаться entries, убедиться, что аудио удалено из Storage. До неё S1 не закрыт. Спайк S0 параллельно теми же ключами.

**Урок дня:** `db push` в проекте с двумя репо не работает ни у кого; путь — `db query -f` + `migration repair` (`scripts/db-migrate.sh`). Правки настроек разрешений агент делать не может, это руками.
